# Streamlit app dependencies (read by Streamlit Community Cloud on deploy).
# Kept minimal on purpose: the Streamlit page only needs the data + plotting stack.
# (FastAPI/uvicorn for the local REST API are installed separately — see README.)
streamlit
pandas
numpy
matplotlib
seaborn==0.13.2
scipy
